{
  "name": "sadraskol",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "post-install": "spago install",
    "test": "spago test",
    "watch": "spago bundle-app --watch --main Main --to index.js",
    "parcel": "parcel index.js --no-hmr",
    "build": "spago bundle-app --main Main --to index.js && parcel build index.js"
  },
  "author": "Thomas Bracher <thomas.bracher@cpe.fr>",
  "license": "ISC",
  "devDependencies": {
    "dart-sass": "^1.22.10",
    "parcel": "^1.12.3",
    "purescript": "^0.13.3",
    "spago": "^0.9.0"
  }
}
